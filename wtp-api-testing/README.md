# 🔌 WTP – API Testing Project

Учебный проект по тестированию REST API и работе с базой данных PostgreSQL.

---

## 📋 О проекте

WTP — это небольшое REST API для работы с пользователями.  
Проект использовался на курсе по тестированию для отработки:

- тестирования API через Postman;
- запуска тестового окружения в Docker;
- проверки результатов запросов через SQL в PostgreSQL (pgAdmin 4).

В этом репозитории я собрала свои артефакты по тестированию API и проверкам данных в базе.

---

## 🎯 Что тестировала

### 1. Управление пользователями (`/users`)

- **Создание пользователей** через `POST /users` (валидные и невалидные данные).
- **Получение списка пользователей** через `GET /users`.
- **Удаление пользователей** через `DELETE /users/{id}`.
- **Массовое создание пользователей** через запуск коллекции в Postman (Runner) для проверки стабильности и ограничений.

Для проверки результатов я использовала не только ответы API, но и SQL‑запросы к таблице `users` в PostgreSQL.  
Примеры запросов — в файле [`sql/verification-queries.md`](sql/verification-queries.md).

---

### 2. Авторизация и токены (`/login`)

- **Успешная авторизация** по правильной паре email/пароль.
- **Неверные данные** (ошибочный email/пароль).
- **Получение токена доступа** через `GET /login` и проверка структуры ответа.
- **Проверка таблицы `tokens`** в БД после логина:
  - создаётся ли запись для нужного пользователя;
  - корректно ли установлено время истечения `expire_at`.

SQL‑проверки также вынесены в [`sql/verification-queries.md`](sql/verification-queries.md).

---

### 3. Работа с базой данных и SQL

Через pgAdmin 4 я:

- проверяла, что данные в таблицах `users` и `tokens` соответствуют ожидаемому поведению API;
- писала простые запросы с фильтрацией, агрегатами и JOIN для учебных таблиц `cities`, `packages`, `winners`;
- использовала SQL, чтобы подтвердить поведение API и зафиксировать расхождения с контрактом.

Отдельный файл с примерами запросов: [`sql/verification-queries.md`](sql/verification-queries.md).
План воспроизведения и список скриншотов: [`screenshots/screenshots-plan.md`](screenshots/screenshots-plan.md).

---

## 🐛 Найденные дефекты и SQL‑наблюдения

Все проверки и доказательства (SQL + скриншоты) собраны в:
- [`sql/verification-queries.md`](sql/verification-queries.md)
- [`screenshots/screenshots-plan.md`](screenshots/screenshots-plan.md)

### Дефект: `role_id` возвращается как массив вместо числа (GET /users)

- В ответе API поле `role_id` приходит как массив, например `[4]`, вместо числового значения.
- В базе в таблице `users` поле `role_id` хранится как обычное число.
- Это нарушает контракт API и может ломать работу фронтенда.

SQL‑запросы помогают увидеть реальное значение `role_id` в базе и сравнить его с тем, что приходит в ответе.

---

### Опционально: security‑наблюдения 

- Проверка хранения паролей в таблице `users`.
- Проверка наличия просроченных токенов в таблице `tokens`.

---

## 🛠 Технологии и инструменты

- **Postman** — тестирование REST API, коллекции запросов, переменные и окружения.
- **Docker** — запуск тестового окружения (API + БД).
- **FastAPI** — фреймворк, на котором реализовано учебное API.
- **PostgreSQL + pgAdmin 4** — просмотр и проверка данных через SQL.
- **REST API, JSON** — работа с форматами и структурами ответов.

---

## 📂 Что лежит в этом разделе портфолио

- `README.md` — описание проекта, сценариев тестирования и найденных багов.
- `sql/verification-queries.md` — примеры SQL‑запросов для проверки данных и воспроизведения багов.
- `screenshots/` — скриншоты из Postman и Docker.


